name: 'GLVD Triage'
description: 'Apply CVE contexts to a GLVD database'
branding:
  icon: command
  color: gray-dark
inputs:
  triage_file:
    description: 'The triage file to apply'
    required: true
  pat:
    description: ''
    required: true
outputs:
  result:
    description: "todo"
runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    GLVD_TRIAGE_FILE: ${{ inputs.triage_file }}
    PAT: ${{ inputs.pat }}

